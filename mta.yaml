_schema-version: "3.2"
ID: quotationinterface
description: Generated by Fiori Tools
version: 0.0.1
modules:
- name: quotationinterface-destination-content
  type: com.sap.application.content
  requires:
  - name: quotationinterface-destination-service
    parameters:
      content-target: true
  - name: quotationinterface-repo-host
    parameters:
      service-key:
        name: quotationinterface-repo-host-key
  - name: quotationinterface-uaa
    parameters:
      service-key:
        name: quotationinterface-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: quotationinterface_html_repo_host
          ServiceInstanceName: quotationinterface-html5-srv
          ServiceKeyName: quotationinterface-repo-host-key
          sap.cloud.service: quotationinterface
        - Authentication: OAuth2UserTokenExchange
          Name: quotationinterface_uaa
          ServiceInstanceName: quotationinterface-xsuaa-srv
          ServiceKeyName: quotationinterface-uaa-key
          sap.cloud.service: quotationinterface
        existing_destinations_policy: ignore
  build-parameters:
    no-source: true
- name: quotationinterface-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: quotationinterface-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - quotationinterface.zip
      name: quotationinterface
      target-path: resources/
- name: quotationinterface
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: quotationinterface-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: quotationinterface-destination-service
    service-plan: lite
- name: quotationinterface-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: quotationinterface-xsuaa-srv
    service-plan: application
- name: quotationinterface-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: quotationinterface-html5-srv
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
